### Dependencies ###
node_modules/
dist/

### Playwright generated output ###
test-results/
test-results-*/
playwright-report/
playwright-report-*/
.playwright/
Test Reports/

### Test report files ###
*.xlsx

### Local environment ###
.env
.env.local
.env.*.local
env.qa-shared.download

### Claude / AI agent internal files ###
.claude/
memory/

### Local-only Slack update drafts — not part of repo history ###
docs/updates/

### Local-only demo recordings (Cloudflare-bypass test videos, etc.) — not part of repo history ###
docs/*.webm

### OS files ###
.DS_Store
Thumbs.db

### IDE ###
.vscode/settings.json
.idea/

# Local Netlify folder
.netlify

# GUI dashboard — not ready for the team yet, local-only until it is
# (decided by Reeve 2026-08-26; remove this line when it's ready to share)
gui/

# QA Automated Regression Results dashboard — generated data + staged
# reports (rebuilt by dashboard/build-data.cjs / deploy-dashboard.cjs from
# local Test Reports/, not meant to live in git) and its local Netlify
# site-link state, same reasoning as .netlify above.
dashboard/public/data.json
dashboard/public/reports/
dashboard/.netlify-site.json
